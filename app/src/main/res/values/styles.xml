<resources>

    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowFullscreen">true
        </item> <!-- Faz o aplicativo ocupar toda a tela -->
        <item name="android:windowTranslucentStatus">true
        </item> <!-- Se você estiver usando status bar -->
        <item name="android:statusBarColor">@android:color/transparent
        </item> <!-- Para ocultar a cor da barra de status -->
    </style>

    <style name="MainActivityTheme.Starting" parent="Theme.AppCompat.NoActionBar">
        <item name="windowSplashScreenBackground">@android:color/black</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="postSplashScreenTheme">@style/AppTheme</item>
    </style>

    <style name="Widget.Avos2.AppWidget.InnerView" parent="android:Widget">
        <item name="android:background">?android:attr/colorBackground</item>
        <item name="android:textColor">?android:attr/textColorPrimary</item>
    </style>
</resources>
